class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n = nums.size() + 1;
        int t = (n * (n-1)) / 2;
        for (int num : nums) {
            t -= num;
        }
        return t;
    }
};
