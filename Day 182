class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> k;
        for(int i=0;i<nums.size();i++){
            k.push_back(nums[i]*nums[i]);
        }
        sort(k.begin(),k.end());
        return k;
    }
};
